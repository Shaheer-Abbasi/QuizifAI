services:
  - type: web
    name: ai-quiz-app
    env: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
